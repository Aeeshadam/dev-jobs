import { StyledHeader, HeaderContainer } from "../styles/Header.styles";

const Header = () => {
  return (
    <HeaderContainer>
      <StyledHeader />
    </HeaderContainer>
  );
};
export default Header;
